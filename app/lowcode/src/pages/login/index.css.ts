import { createStyles } from "@mantine/core";

export default createStyles(() => ({
  login: {
    display: "flex",
    justifyContent: 'center',
    alignContent: 'center',
    height: '100vh',
    width: '100vw'
  }
}));
